import React from 'react';

const Form = (props) => {
    return (
        <form>
            <input />
            <input />
            <button>Add</button>
        </form>
    );
}

export default Form;